# crawler 로컬 직접 실행 시 사용 (Docker 외부)
# DB는 docker-compose로 먼저 기동해야 함: docker-compose up db

DB_HOST=localhost
DB_PORT=3306
DB_NAME=crawlerdb
DB_USER=crawler
DB_PASSWORD=crawler1234

# 개발 시 빠른 확인을 위해 60초로 설정
CRAWL_INTERVAL_SEC=60
